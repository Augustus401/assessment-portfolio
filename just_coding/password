UN = "admin"
CORRECT = "1234"
MAX = 3

for attempt in range(1, MAX + 1):
    name = input("")
    pw = input(f"Enter password: ")

    if pw == CORRECT and name == UN:
        print(f"hello {name}")
        break
    else:
        print("Try again")
else:
    print("sorry you failed to log in")
